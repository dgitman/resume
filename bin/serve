#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "Starting Jekyll at http://localhost:4000 (Ctrl+C to stop)"
echo "Restart the server after editing _config.yml"
bundle exec jekyll serve --livereload "$@"
